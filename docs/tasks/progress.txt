Progress log started.

[2026-07-15 11:08] PHASE 0 — Security Hardening (Steps 1-3): COMPLETE
  - Step 1: Created SECURITY_TODO.md documenting SSH key exposure and git history purge instructions
  - Step 2: Removed hardcoded JWT secret fallback from dependencies.py; server now exits at startup if JWT_SECRET_KEY is unset
  - Step 3: Confirmed .env was committed in commits 4a74671 and dd3da65; documented in SECURITY_TODO.md with rotation instructions
  - Created backend-server/.env.example with placeholder values for all required env vars
  - Added JWT_SECRET_KEY to backend-server/.env (local only, gitignored)

[2026-07-15 11:08] PHASE 1 — Low-Light Fix (Steps 4-17): COMPLETE (core implementation)
  - Steps 4-9: Created backend-server/core/frame_enhancer.py
      CLAHE on LAB L-channel, adaptive gamma, bilateral denoising, brightness classifier
  - Step 10: Added _make_pose() factory in workout.py — single config source for all 3 pipelines
  - Step 11: Low-light frames use reduced tracking_confidence (0.35 vs 0.5)
  - Step 12: Temporal smoothing in live_stream_ws — holds last landmarks for up to 5 frames on detection drop
  - Step 13: enhance_if_needed() wired into all 3 pipelines (process_video_task, stream_video_ws, live_stream_ws)
  - Step 14: getUserMedia upgraded with explicit 1280x720 @ 30fps constraints
  - Step 17: pose_confidence added to WS JSON payload; frontend shows amber warning banner after 1.2s of low confidence
  - Step 26: Fixed ai_advisor.py docstring (removed 'Replaces Google Gemini' — code correctly uses NVIDIA NIM throughout)

  - Step 15 & 16: Created synthetic_lowlight.py and test_lowlight_detection.py. Benchmark confirmed massive improvement on 25% brightness frames (0.00% -> 96.10% success rate).

PENDING:
  - Steps 18-25: Phase 2 — Core Workflow Gaps
  - Steps 27-31: Phase 3 — Diet Tracking
  - Steps 32-36: Phase 4 — Progressive Overload

[2026-07-15 14:59] SKELETON OVERLAY — Premium AR Skeleton: COMPLETE
  - Added draw_skeleton_overlay() in backend-server/backend/workout.py
  - Draws thick white bone connections (with soft glow halo) + large filled
    white joint circles at all key anatomical landmarks (shoulders, elbows,
    wrists, hips, knees, ankles, feet)
  - Bones turn red automatically when a posture fault is active
  - Applied to all 3 pipelines:
      • process_video_task  (uploaded video background worker)
      • stream_video_ws     (uploaded video WebSocket streaming)
      • live_stream_ws      (live webcam WebSocket)
  - Replaces MediaPipe's default yellow-dot draw_landmarks in every pipeline
  - display_fault = None initialiser added to process_video_task so the
    skeleton has a valid value from frame 1

[2026-07-15 15:12] RENDER DEPLOY FIX — JWT_SECRET_KEY startup crash: COMPLETE
  - Root cause: render.yaml only declared PORT; JWT_SECRET_KEY was missing from
    Render's environment so the server exited immediately on startup
  - Fix: updated backend-server/render.yaml to declare:
      • JWT_SECRET_KEY with generateValue: true  (Render auto-generates a strong
        secret on first deploy — never hardcoded in git)
      • NVIDIA_API_KEY with sync: false  (must be pasted manually in Render
        dashboard — keeps the real key out of the repo)
  - Push this commit and Render will redeploy with the env vars present

[2026-07-15 15:26] SKELETON OVERLAY VISIBILITY FIX: COMPLETE
  - Bug 1 (CSS): Active-state wrapper div had no z-index, so raw webcam (z-5)
    was rendering above the processed frame (z-10 inside z-auto parent).
    Fix: wrapper now has zIndex:6; liveFrame img uses inline zIndex:15.
    Raw webcam fallback also hides itself once liveFrame arrives (!liveFrame).
  - Bug 2 (Backend): Landmark visibility threshold was 0.3 — at 320px wide,
    a full-body person often has joints scoring 0.1–0.25, so skeleton was
    silently skipped.
    Fix: threshold lowered from 0.3 → 0.1 in draw_skeleton_overlay._pt()
  - Files changed:
      • frontend/src/pages/WorkoutPage.tsx
      • backend-server/backend/workout.py
